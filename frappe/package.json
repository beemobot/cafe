{
	"name": "frappe",
	"version": "0.0.1",
	"description": "A gRPC proxy for Beemo.",
	"author": "BeemoBot",
	"license": "MIT",
	"type": "module",
	"scripts": {
		"build": "rimraf dist && tsc",
		"start": "node --enable-source-maps dist/index",
		"bs": "npm run build && npm run start",
		"clean": "rimraf dist",
		"format": "prettier --write .",
		"lint": "eslint . --fix"
	},
	"dependencies": {
		"dotenv": "^16.4.7",
		"pino": "^9.6.0",
		"pino-pretty": "^13.0.0"
	},
	"devDependencies": {
		"@eslint/compat": "^1.2.5",
		"@eslint/js": "^9.18.0",
		"@types/node": "^22.10.5",
		"eslint": "^9.18.0",
		"eslint-config-prettier": "^9.1.0",
		"eslint-plugin-import": "^2.31.0",
		"prettier": "^3.4.2",
		"rimraf": "^6.0.1",
		"tslib": "^2.8.1",
		"typescript": "^5.7.3",
		"typescript-eslint": "^8.19.1"
	}
}
